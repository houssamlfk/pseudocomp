package com.school.projects;

enum TokType {
    // you need to find the token dictionary of pseudo code, you gotta choose a
    // certain model.
    DECLAR, IDENTIFIER, INCREMENT, DECREMENT, PLUS, MINUS, STAR, SLASH, PRINT, READ, // etc idk you complete
}
